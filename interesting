Si pasamos null por el operador typeof, nos dara como salida objeto, y es un bug reconocido
Con las variables var, no existen los ambitos de variable o scopes
Valores que comparados son iguales, dan diferente salida en el metodo Boolean()
